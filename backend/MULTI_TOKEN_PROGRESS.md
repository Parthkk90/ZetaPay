Multi-token support progress

- Contract: token whitelist mapping added to `UniversalPayment.sol`.
- Backend: payment create route now accepts `token` param and validates merchant accepted tokens.
- Next: deploy updated contract to testnet, update frontend token selector, add tests.
